/**
 * 框架依赖的分包数据
 */
export default class FrameSubpackages {
    /** 分包列表,必须和分包配置文件中的列表一致 */
    public static subpackages: {
        //包名字
        name: string,
        //路径
        root: string,
    }[] = [
            //
        ];
}